Search on recursive mode for world "transition-all overflow-hidden" in all files inside the current path "./"  
  grep -rwi --color=auto --binary-files=without-match --exclude-dir={node_modules,dist,.git} "transition-all overflow-hidden" ./

